export class Mesa {
  idMesa: string;
  codigo: string;
  mesa: string;

  constructor(idMesa: string, codigo: string, mesa: string) {
    this.idMesa = idMesa;
    this.codigo = codigo;
    this.mesa = mesa;
  }
}
