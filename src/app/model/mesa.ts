export class Mesa {
  id_mesa: string;
  codigo: string;
  mesa: string;

  constructor(id_mesa: string, codigo: string, mesa: string) {
    this.id_mesa = id_mesa;
    this.codigo = codigo;
    this.mesa = mesa;
  }
}
